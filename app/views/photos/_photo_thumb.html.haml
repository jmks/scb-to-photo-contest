= link_to photo_path(photo), style: 'text-decoration: none;' do
  -# photo or icon
  - if photo.thumbnail_url?
    .img-responsive
      = image_tag photo.thumbnail_url
  - else
    %i#placeholder-thumbnail.fa.fa-picture-o.fa-4x

  -# title
  - if photo.title?
    %p= photo.title
.stats
  %span
    %i.fa.fa-thumbs-up
    = photo.votes
  %span
    %i.fa.fa-eye
    = photo.views