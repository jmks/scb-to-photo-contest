- if Winner.assignments_remaining?
  %h3.text-center We are currently allocating prizes and contacting winners. Contest winners will be updated shortly!
- else
  / static page for now
  .container#photos

    .two-tone-title.text-center
      %span Our
      %span Winners

    #winners-photo-wall.row
      .col-xs-12
        %ul.text-center.list-inline
          - @winner_by_award.each_pair do |prize, photo|
            %li.photo
              = link_to photo_path(photo), style: 'text-decoration: none;' do
                = image_tag photo.thumbnail_sm_url, alt: photo.title
                .thumb-title
                  %p= pp_prize(prize)